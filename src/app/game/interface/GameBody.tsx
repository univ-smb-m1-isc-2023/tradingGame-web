export interface GameBody {
    title: string;
    type: string;
    initialDate: string;
    finalDate: string;
    initialBalance: number;
    moveDuration: string;
    adminId: string;
    playerIds: number[];
  }